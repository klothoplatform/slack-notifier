{
  "devDependencies": {
    "@octokit/webhooks-types": "^6.3.5",
    "@slack/web-api": "^6.7.2",
    "@types/express": "^4.17.13",
    "@types/jest": "^28.1.7",
    "jest": "^28.1.3",
    "jest-ts-auto-mock": "^2.1.0",
    "ts-auto-mock": "^3.6.2",
    "ts-jest": "^28.0.8",
    "ttypescript": "^1.5.13",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "test": "jest",
    "klotho:build": "npm test && npx tsc && klotho dist --strict --app \"${KLOTHOBOT_APP_NAME:-klothobot}\" && (cd compiled && npm install)",
    "klotho:deploy": "pulumi up -C compiled"
  },
  "dependencies": {
    "@slack/webhook": "^6.1.0",
    "express": "^4.18.1"
  }
}
